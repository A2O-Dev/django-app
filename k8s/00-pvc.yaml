apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-app-demo
  namespace: demos
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      type: django-app-demo
